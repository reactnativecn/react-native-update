version: 2
updates:
  # Actions are pinned to commit SHAs in the workflows; Dependabot keeps the
  # SHA + version comment pairs current instead of a human copying hashes.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns:
          - "*"
